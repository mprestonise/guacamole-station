{
  "name": "guacamole-station",
  "version": "1.0.0",
  "description": "A tool for evaluating duplicates against a list of issues",
  "engines": {
    "node": "8.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mprestonise/guacamole-station"
  },
  "keywords": [
    "node",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}
